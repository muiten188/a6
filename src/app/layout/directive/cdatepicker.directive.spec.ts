import { CdatepickerDirective } from './cdatepicker.directive';

describe('CdatepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new CdatepickerDirective();
    expect(directive).toBeTruthy();
  });
});
